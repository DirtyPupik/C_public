#include <iostream>

using std::cout;
using std::cin;


int main(){
    // condition ? if true : if false;
    int grade = 75;
    grade >= 75 ? cout << "good" : cout << "not that good" ;

    return 0;
}